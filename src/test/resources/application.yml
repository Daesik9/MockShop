spring:
  profiles:
    active: ${profile}
  config:
    import: aws-parameterstore:/mockshop/test/
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${mail.username}
    password: ${mail.password}
    properties:
      mail:
        smtp:
          auth: true
          timeout: 5000
          starttls:
            enable: true

file:
  dir: ${file.dir}
