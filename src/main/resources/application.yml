file:
  dir: /Users/personal/Documents/Study/project/mockshop-file/

spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/mockshop
    username: sa
    password:
    driver-class-name: org.h2.Driver
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
  profiles:
    active: local

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: trace
    org.hibernate.orm.jdbc.bind: trace